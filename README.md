# setup

```
mkdir html5 //폴더생성
cd html5  // 폴더 들어감

npm browser-sync -g
npm install gulp -g 

npm init -y 
npm install gulp --save-dev
npm install browser-sync --save-dev
npm install pixi.js --save

```